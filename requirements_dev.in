bdd-tester==0.0.6
lxml
pytest
pytest-cov

# bdd-tester 0.0.6 expects the pre-5 gherkin parse tree. From v5 onwards scenarios
# are nested differently under feature['children'] and BDDTester._gherkinify_feature
# fails, so this stays pinned until the runner is updated.
gherkin-official<5
